# 项目决策记录

> 记录"为什么这样选，而不是那样选"。  
> 使用 ADR（Architecture Decision Record）格式，每条包含编号、状态、背景、方案、决策和后果。  
> 状态说明：**Accepted** = 已采纳生效；**Proposed** = 提议待讨论；**Deprecated** = 已被新决策替换。

---

## ADR-001：采用 Spring Boot 单体架构

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 项目是学习型 MVP，服务器配置为 2 核 2G，业务量很小。
- **候选方案：**
  1. Spring Boot 单体
  2. Spring Cloud 微服务
- **决策：** 采用 Spring Boot 单体 + 模块化分包。
- **原因：**
  - 功能规模小，没有独立部署和扩缩容需求；
  - 微服务增加服务注册、网关、配置中心、链路追踪等运维复杂度；
  - 当前学习重点应放在业务闭环、权限、事务和订单状态上。
- **后果：** 开发和部署简单，适合单机服务器，调试和理解容易。后续业务增长时需要重新评估拆分。

---

## ADR-002：采用数据库唯一索引作为并发预约最终保障

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 同一阿姨同一小时块只能被一位阿姨抢到，需要解决并发冲突。
- **候选方案：**
  1. 数据库唯一索引 + 事务
  2. Redis 分布式锁
  3. 先查再插入（无锁方案）
- **决策：** 采用方案 1：数据库唯一索引 + 事务 + 订单状态条件更新。
- **原因：**
  - 唯一索引是数据库层面的原子性保障，可靠且容易理解；
  - Redis 分布式锁增加复杂度和运维成本，第一版业务量不需要；
  - "先查再插入"在并发下存在竞态条件，不可靠。
- **后果：** 需要在代码中捕获 `DuplicateKeyException` 并转换为友好的业务提示。

---

## ADR-003：第一版采用 9 状态订单状态机

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 需要支持阿姨抢单、阿姨完成服务、用户确认、投诉等完整流程。
- **候选方案：**
  1. 5 状态：待支付 → 待服务 → 待评价 → 已完成 → 已取消
  2. 9 状态：待支付 → 待抢单 → 待服务 → 服务中 → 待确认 → 待评价 → 已完成 → 已取消 → 投诉中
- **决策：** 采用 9 状态。
- **原因：** 5 状态无法区分阿姨抢单、服务中、用户确认等环节，不利于状态追踪和权限控制。
- **后果：** 状态流转逻辑更复杂，但业务表达更精确，每个角色的操作边界更清晰。

---

## ADR-004：阿姨端与用户端共用同一个 Vue 前端工程

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 需要支持用户和阿姨两种角色，但第一版不做独立 App。
- **候选方案：**
  1. 三个独立前端工程（用户端 / 阿姨端 / 管理后台）
  2. 两个前端工程（用户阿姨共用 + 管理后台）
  3. 一个前端工程（三个角色路由分发）
- **决策：** 采用方案 3。
- **原因：** 前端代码量不大，三个角色共用登录、订单列表等组件，减少重复开发。
- **后果：** 前端打包体积会包含所有角色的代码，但第一版可以接受。后续可根据复杂度拆分。

---

## ADR-005：第一版不使用超时自动取消

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 原计划待支付订单 30 分钟自动取消。
- **候选方案：**
  1. Spring Task 每分钟扫描 + 自动取消
  2. 不设自动取消，仅保留用户主动取消
- **决策：** 采用方案 2。
- **原因：** 第一版是学习型项目，没有大量真实订单，自动取消功能的学习价值低于其他核心功能。
- **后果：** 待抢单订单可能长期存在无人处理的状态，但管理员可以手动介入。

---

## ADR-006：第一版不做独立地址表

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 原计划有地址管理功能（CRUD + 默认地址）。
- **候选方案：**
  1. 独立 address 表，支持 CRUD
  2. 地址直接作为订单字段保存，不做地址管理
- **决策：** 采用方案 2。
- **原因：** 地址 CRUD 的复杂度不高但有重复的增删改查工作量，第一版订单流程更需要优先实现。
- **后果：** 后续可以通过表结构变更将地址字段抽取为独立表，升级为完整地址管理。

---

## ADR-007：采用整点小时块替代固定时段

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 原计划固定上午（09-12）和下午（14-17）两个时段。
- **候选方案：**
  1. 固定两个 3 小时时段
  2. 用户自由选择开始时间 + 时长（整点小时块）
- **决策：** 采用方案 2。
- **原因：** 小时块更灵活，能覆盖更多服务和演示场景；档期控制逻辑也更通用，未来扩展时不需要重构。
- **后果：** 档期表设计需要按小时块存储，比固定时段更复杂，但灵活性更高。

---

## ADR-008：阿姨状态分离为管理与接单两个字段

- **状态：** Accepted
- **日期：** 2026-07-12
- **背景：** 阿姨需要同时支持管理员控制（上下架/禁用）和阿姨自主控制（休息/接单），以及系统控制的档期占用。
- **候选方案：**
  1. 一个字段混合表达所有状态
  2. 两个独立字段：管理状态 + 接单状态，档期由独立表管理
- **决策：** 采用方案 2。
- **原因：** 一个字段无法区分"管理员下架"和"阿姨休息"——前者用户不可见，后者只是阿姨自主设置。混合表达会导致逻辑混乱和权限漏洞。
- **后果：** 需要两个字段 + 一个档期表，模型更清晰但字段更多。

---

## ADR-009：阿姨自行注册，管理员只负责运营状态管理

- **状态：** Accepted
- **日期：** 2026-07-12
- **决策：** 阿姨通过公开注册接口自行创建账号和一对一资料，管理员不能新增或代注册阿姨，只能编辑资料、上下架、禁用和逻辑删除。
- **安全约束：** 公开注册角色只允许 USER/AUNT；ADMIN 仅由初始化 SQL 创建；角色由后端白名单校验。
- **后果：** 注册事务需要同时创建 `sys_user` 与 `aunt`；存在历史订单的阿姨禁止物理删除。

---

## ADR-010：拆分订单归属并发与阿姨档期并发保障

- **状态：** Accepted
- **日期：** 2026-07-12
- **决策：** 同一订单只能被抢一次由订单状态条件更新保障；同一阿姨同一小时块冲突由联合唯一索引保障，两者在同一事务内执行。
- **后果：** 必须校验条件更新行数，档期插入失败时回滚订单归属。

---

## ADR-011：限制取消范围并规范投诉闭环

- **状态：** Accepted
- **日期：** 2026-07-12
- **决策：** 仅待支付、待抢单、待服务允许取消；服务中和待确认由管理员线下介入；仅待评价可在系统内投诉一次。
- **后果：** 取消和投诉都必须使用状态条件更新；投诉处理后不再允许评价。

---

## ADR-012：敏感配置仅通过环境变量注入

- **状态：** Accepted
- **日期：** 2026-07-12
- **决策：** JWT 密钥、数据库密码、Redis 密码和管理员初始密码不进入源码、日志或 Git；生产环境不暴露 MySQL/Redis 端口。
- **后果：** 必须维护 `.env.example`、`.gitignore`、密钥轮换流程、容器健康检查和资源限制。

---

## ADR-013：ADMIN 账号由应用启动初始化，密码走环境变量

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 阶段 1 需要创建管理员账号。规范要求"管理员账号仅由初始化 SQL 创建"，但 ADR-012 要求"管理员初始密码不得写入源码、日志或 Git"。
- **候选方案：**
  1. init.sql 写明文密码插入 ADMIN
  2. init.sql 写固定 BCrypt 哈希插入 ADMIN（对应固定默认密码）
  3. 应用启动时由 ApplicationRunner 用环境变量密码 BCrypt 加密后插入（init.sql 只建表）
- **决策：** 采用方案 3。
- **原因：**
  - 方案 1 明文密码进 SQL 文件入 Git，直接违反 ADR-012；
  - 方案 2 固定哈希入 Git，虽不可逆但可离线爆破，且默认密码固定存在安全风险；
  - 方案 3 密码只走环境变量 `ADMIN_INIT_PASSWORD`，BCrypt 哈希入库，符合 ADR-012「敏感配置仅环境变量注入」；建表仍由 SQL（schema.sql）完成，ADMIN 创建是系统初始化逻辑而非注册接口，不违反"管理员不开放注册"。
- **后果：**
  - 部署时必须设置 `ADMIN_INIT_PASSWORD` 环境变量，否则启动跳过 ADMIN 初始化并日志告警；
  - 幂等：已存在 ADMIN 账号则跳过，可重复启动；
  - 默认手机号 `13800000000`，可由 `ADMIN_PHONE` 环境变量覆盖。

---

## ADR-014：JWT 密码哈希签名使改密码后旧 token 失效

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 无状态 JWT 改密码后旧 token 仍有效直到过期，存在安全风险（token 泄露后改密码无法立即止损）。
- **候选方案：**
  1. Redis JWT 黑名单（改密码时把旧 token 加入黑名单）
  2. token_version 版本号字段（sys_user 加字段，改密码时 +1，校验时比对）
  3. 密码哈希签名（JWT claims 存密码哈希前 16 位，Filter 校验时比对数据库当前哈希）
- **决策：** 采用方案 3。
- **原因：**
  - 方案 1 不适用：找回密码接口（手机号 + 验证码）不携带旧 token，无法获取要拉黑的 token；
  - 方案 2 需给 sys_user 加字段 + 改 resetPassword 逻辑，改动较大；
  - 方案 3 无需改表结构，改密码后 BCrypt 重新生成 salt，哈希自然变化，旧 token 的 pwdSig 自动失效，改动最小。
- **后果：**
  - 每次受保护请求查一次 sys_user 比对 pwdSig（MVP 可接受，后续可 Redis 缓存降低 DB 压力）；
  - JWT claims 含密码哈希前 16 位（BCrypt 哈希片段，含 salt 前缀，非明文密码，安全性可接受）；
  - 改密码后旧 token 立即失效，新登录获取的新 token 有效。

---

## ADR-015：aunt 逻辑删除用 MyBatis-Plus @TableLogic

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 规范要求"阿姨删除默认采用逻辑删除，存在历史订单时禁止物理删除"。需要选择逻辑删除实现方式。
- **候选方案：**
  1. 手动 update deleted 字段（每次查询手动加 deleted=0 条件）
  2. MyBatis-Plus @TableLogic 注解（查询自动过滤 deleted=1，删除自动 update deleted=1）
- **决策：** 采用方案 2。
- **原因：**
  - 方案 1 每个查询都要手动加条件，容易遗漏，导致已删除阿姨泄露到列表；
  - 方案 2 框架自动处理，查询自动追加 `deleted=0`，删除自动 `update deleted=1`，全局配置 `logic-delete-field/value`，一致且不易出错。
- **后果：**
  - aunt 表加 `deleted TINYINT` 字段，Aunt 实体加 `@TableLogic`；
  - `deleteById` 自动变为 `update deleted=1`，`selectPage/selectList/selectById` 自动过滤 `deleted=1`；
  - 逻辑删除前检查进行中订单（待服务/服务中/待确认），存在则拒绝删除，避免阿姨被删后订单卡死（2026-07-13 review 后补充）。

---

## ADR-016：@PreAuthorize 异常由 GlobalExceptionHandler 处理为 403

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 阶段2启用 `@EnableMethodSecurity` + `@PreAuthorize` 角色隔离后，越权请求返回 500 而非 403。
- **原因分析：**
  - `@PreAuthorize` 在 Controller 方法层（AOP 拦截）抛 `AccessDeniedException`；
  - 该异常向上抛到 `DispatcherServlet`，被 `@RestControllerAdvice`（GlobalExceptionHandler）的 `@ExceptionHandler(Exception.class)` 捕获，返回 500；
  - Security 的 `RestAccessDeniedHandler` 只处理 Filter 层异常，捕获不到 Controller 层的 `AccessDeniedException`。
- **决策：** 在 GlobalExceptionHandler 加 `@ExceptionHandler(AccessDeniedException.class)` 返回 403。
- **原因：** Controller 层的权限异常由 @ControllerAdvice 统一处理，与 Filter 层的 RestAccessDeniedHandler 分工清晰，互不冲突。
- **后果：**
  - @PreAuthorize 拒绝 → GlobalExceptionHandler 返回 HTTP 403 + body code=403（@ResponseStatus 统一 HTTP 状态码，2026-07-13 review 后修正）；
  - Filter 层无权限（如 URL 级别）仍由 RestAccessDeniedHandler 返回 HTTP 403；
  - 两套机制并存，HTTP 状态码一致（均为 403），分别覆盖方法级和 URL 级权限控制。

---

## ADR-017：抢单校验阿姨接单状态，管理员指派不校验

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 规范 §7.3 抢单事务步骤只提"校验阿姨管理状态为可用"，未明确是否校验接单状态。规范 §4 阿姨角色支持"设置个人接单状态（可抢单/休息）"。
- **候选方案：**
  1. 抢单只校验管理状态，不校验接单状态（休息也能抢）
  2. 抢单同时校验管理状态=可用 + 接单状态=可抢单
  3. 管理员指派也校验接单状态
- **决策：** 采用方案 2（抢单校验接单状态），管理员指派采用方案 1 的反面（不校验接单状态）。
- **原因：**
  - 方案 1 使"设置休息"形同虚设，阿姨休息时仍能抢单，违反规范 §4 设计意图；
  - 方案 2 让接单状态真正生效，阿姨休息时抢单被拒并提示切换为可抢单；
  - 管理员指派是兜底机制，应能强制指派任何管理状态可用的阿姨，不论阿姨是否休息，故不校验接单状态。
- **后果：**
  - 阿姨抢单前需确保 accept_status=AVAILABLE；
  - 管理员指派不受理 accept_status，但受理 admin_status（下架/禁用不可指派）。

---

## ADR-018：待支付/待抢单订单不占用档期

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 规范 §5.3 明确"待支付订单和待抢单订单不占用任何阿姨档期；阿姨抢单或管理员指派成功时才锁定档期"。
- **决策：** 档期记录仅在抢单/指派事务成功时插入；取消待服务订单时删除档期释放；待支付/待抢单/待支付取消不涉及档期。
- **原因：** 待支付/待抢单阶段阿姨尚未确定，无法锁定具体阿姨的档期；提前占用会导致其他阿姨无法接同时段订单，且订单可能被取消造成无谓的档期锁定与释放。
- **后果：**
  - `aunt_booking_slot` 仅在订单状态=待服务(2)及之后存在记录；
  - 取消逻辑只需对待服务订单执行档期释放（待支付/待抢单无档期）；
  - 抢单事务内"条件更新订单 + 插入档期"原子执行，档期唯一索引冲突时回滚订单更新（ADR-010）。

---

## ADR-019：投诉处理后不更新阿姨评分

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 规范 §7.9 明确"管理员处理后订单进入已完成，不再允许评价，也不更新阿姨评分"。
- **决策：** 投诉处理流程只更新投诉状态与订单状态（投诉中→已完成），不触发阿姨评分与服务次数更新。
- **原因：** 投诉意味着服务存在争议，不应按正常评价逻辑给阿姨加分；评分只应反映真实完成的评价，避免投诉订单污染阿姨评分。
- **后果：**
  - 投诉处理的订单：阿姨 rating 与 service_count 不变；
  - 评价流程（ReviewService）才更新阿姨评分，且仅待评价订单可评价；
  - 投诉后订单已是已完成，无法再评价，评分自然不受影响。

---

## ADR-020：评价评分采用读-算-写

- **状态：** Deprecated（已被 ADR-021 替换）
- **日期：** 2026-07-13
- **背景：** 评价后需更新阿姨平均评分与服务次数。公式：newRating = (oldRating * oldCount + newRating) / (oldCount + 1)。
- **候选方案：**
  1. 读-算-写：先查 aunt 当前 rating/count，计算新值，再 updateById
  2. SQL 原子更新：`UPDATE aunt SET rating=(rating*service_count+?)/(service_count+1), service_count=service_count+1 WHERE id=?`
  3. Redis 缓存评分，异步落库
- **决策：** 原采用方案 1（读-算-写），2026-07-13 review 发现并发丢失更新风险后改为方案 2（见 ADR-021）。
- **原因：**
  - MVP 并发量小，同一阿姨同时被评价的概率极低，读-算-写的竞态风险可接受；
  - 方案 1 代码可读性最高，便于学习理解评分计算逻辑；
  - 方案 2 SQL 原子更新虽更安全，但 BigDecimal 除法精度在 SQL 层不易控制；
  - 方案 3 过度设计，MVP 不需要。
- **后果：**
  - 极端并发下阿姨评分可能有微小偏差（MVP 可接受）；
  - 后续业务量增长可改方案 2 或加乐观锁。
- **废弃原因：** 代码 review 发现两个用户同时评价同一阿姨（不同订单）会丢失评分更新，service_count 也会少加。已改用 ADR-021 的 SQL 原子更新方案。

---

## ADR-021：评价评分改用 SQL 原子更新

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** ADR-020 的读-算-写方案在并发下会丢失更新（两个用户同时评价同一阿姨的不同订单，后写覆盖先写，rating 和 service_count 都少算）。
- **决策：** 改用方案 2（SQL 原子更新）。在 `AuntMapper` 加自定义 `@Update` 方法 `updateRatingAndCount`，单条 SQL 原子完成评分加权平均和服务次数 +1。
- **原因：**
  - 单条 SQL 的 UPDATE 是数据库行锁级别的原子操作，天然避免读-算-写的竞态；
  - MySQL `ROUND((rating * service_count + ?) / (service_count + 1), 1)` 精度可控，与 Java BigDecimal HALF_UP 结果一致；
  - 改动小：只需一个 Mapper 自定义方法 + Service 调用处替换，无需改表结构或加乐观锁版本号。
- **后果：**
  - 评分更新并发安全，不再丢失；
  - `ReviewService.updateAuntRating` 不再需要手动读-算-写和 BigDecimal 计算；
  - newRating 参数为 Integer（1-5 校验过），SQL 拼接安全。

---

## ADR-022：MVP 裁剪阿姨按日期/小时块设置不可接单

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 规范 §4 阿姨角色列出"设置具体日期和小时块不可接单"，但当前仅实现了整体接单状态切换（AVAILABLE/RESTING），未实现按日期/小时块的细粒度不可接单设置。
- **决策：** MVP 不实现按日期/小时块设置不可接单，仅保留整体接单状态切换。规范 §4 已标注裁剪。
- **原因：**
  - 按日期/小时块设置不可接单需要新增接口 + 前端日历交互，工作量较大；
  - MVP 核心目标是订单闭环和状态机，整体接单状态已能满足抢单流程；
  - `aunt_booking_slot` 表设计上可支撑此功能（档期占用记录），后续扩展不需改表结构。
- **后果：**
  - 阿姨只能整体切换可抢单/休息，不能按日期/小时块精细控制；
  - 后续可在阶段 5+ 补充接口 `PATCH /api/aunts/me/slots/unavailable`，复用 aunt_booking_slot 表。

---

## ADR-023：MVP 裁剪指定阿姨订单模式

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 规范 §5.2 提到"指定阿姨订单（辅助）：按阿姨当前标价计算"，但当前仅实现了抢单模式（用户发布订单 → 阿姨抢单），未实现用户直接指定某阿姨下单的模式。
- **决策：** MVP 不实现指定阿姨订单模式，仅保留抢单模式。规范 §5.2 已标注裁剪。
- **原因：**
  - 指定阿姨模式需要新增创建订单接口参数（auntId）+ 按阿姨标价计算金额 + 指派逻辑，与抢单流程分支较多；
  - MVP 核心是抢单事务和并发安全，指定阿姨模式是辅助功能，优先级低；
  - aunt 表的 price 字段已保留，后续扩展不需改表结构。
- **后果：**
  - 用户只能发布订单等待抢单，不能直接指定阿姨；
  - aunt.price 字段当前仅用于列表展示和排序，未参与订单计算；
  - 后续可在阶段 5+ 补充指定阿姨下单接口，CreateOrderRequest 加 auntId 字段。

---

## ADR-024：模拟上传采用本地文件 + WebMvcConfig 静态映射

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** MVP 阶段需要模拟图片上传功能（阿姨提交服务完成照片），但规范 §8 明确禁止"文件真实上传/OSS"。需要一种既简单又可验证的上传方案。
- **决策：** 使用 `MockUploadController`（POST /api/upload/mock）接收 MultipartFile，保存到 `backend/uploads/` 目录；通过 `WebMvcConfig` 将 `/mock-uploads/**` 映射为静态资源路径。SecurityConfig 将 `/mock-uploads/**` + `/api/upload/**` 加入 permitAll 白名单。
- **原因：**
  - 本地文件落盘比纯 mock URL（无文件）更真实，可验证预览/展示全流程；
  - WebMvcConfig 静态映射是 Spring Boot 标准做法，无需额外依赖；
  - permitAll 白名单仅用于 MVP 测试，后续接 OSS 时移除。
- **后果：**
  - `uploads/` 目录已加入 .gitignore，不污染仓库；
  - 前端 Vite 需配置 `/mock-uploads` 代理到后端 8080（T-012）；
  - 后续接真实 OSS 时，替换 MockUploadController 即可，前端上传接口无需改动。

---

## ADR-025：阿姨自助编辑个人资料权限变更

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 阶段 2 仅实现管理员编辑阿姨资料（PUT /api/admin/aunts/{id}），阿姨自己只能切换接单状态。用户反馈阿姨端应能自助完善个人资料（姓名、价格、年龄、入行年限、技能标签、简介）。
- **决策：** 新增 `GET/PUT /api/aunts/me/profile` 端点（@PreAuthorize("hasRole('AUNT')")），允许阿姨查看和编辑自己的运营字段（name/price/age/experience/skillTags/intro）。管理员仍保留编辑权限（AuntUpdateRequest 同步加 age/experience）。阿姨列表和详情 DTO 同步补充 age/experience 字段。
- **原因：**
  - 阿姨自主维护资料是家政平台的常见需求，减轻管理员负担；
  - age/experience 字段供用户选阿姨时参考（列表/详情页展示），非敏感信息；
  - 系统维护字段（rating/serviceCount/adminStatus/acceptStatus）阿姨不可编辑，权限边界清晰。
- **后果：**
  - 管理员 PUT /api/admin/aunts/{id} 和阿姨 PUT /api/aunts/me/profile 的编辑范围一致（不含系统字段）；
  - 列表/详情 DTO（AuntListItem/AuntDetail/AuntProfileResponse）均包含 age/experience；
  - 前端阿姨端新增"个人中心"页面。

---

## ADR-026：Vite 前端代理 /mock-uploads 到后端

- **状态：** Accepted
- **日期：** 2026-07-13
- **背景：** 开发模式下 Vite（5173）和 Spring Boot（8080）运行在不同端口。模拟上传返回的相对路径 `/mock-uploads/uuid.jpg` 从前端请求时会走 Vite（5173）而非后端（8080），导致图片 404 裂图。
- **决策：** 在 `vite.config.ts` 的 proxy 配置中增加 `/mock-uploads` → `http://127.0.0.1:8080`，使前端能通过代理访问后端的模拟上传文件。
- **原因：**
  - 前后端分离开发的标准做法，Vite proxy 是官方推荐方案；
  - 不改变后端 URL 生成逻辑（仍返回相对路径），保持代码简洁；
  - 生产部署时由 Nginx 统一代理，无此问题。
- **后果：**
  - 开发模式下上传图片可正常预览和展示（见 T-012 排查过程）；
  - 新增代理规则后需重启 Vite（`npm run dev`）生效；
  - 生产环境省略此配置，由 docker-compose 的 nginx 处理。
